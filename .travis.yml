language: java
osx_image: xcode8.3
os: osx

install:
    - pod repo update
    - pod install

script:
    - xcodebuild -workspace PlacesToVisit.xcworkspace -scheme PlacesToVisit -destination 'platform=iOS Simulator,name=iPhone 6s,OS=11.1.1' clean build test | xcpretty
